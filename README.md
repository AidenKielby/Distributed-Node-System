# Distributed-Node-System
A computer “node” system where the “master” can send out things to be computed to the connected “nodes.” All nodes and the master must be a working computer. Works with all OS
